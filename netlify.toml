[build]
  publish = "build"
  functions = "netlify/functions"

[functions]
  node_bundler = "esbuild"

[build.environment]
  # ODDS_API_KEY should be set as environment variable in Cloudflare Pages settings

